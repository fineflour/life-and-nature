%section.register.section
  .container
    .row
      .col-lg-12
        .section-title.tpadding-2
          %h2
            Sign In
          %img{src: "/assets/section-img.png", alt: "#"}
          %p 
            Our acupuncture services have been proven effective by many patients over the years. We are highly specialized in treating many difficult diseases and symptoms of unknown cause.

    .row
      .col-12
        = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .form-inputs
            = f.input :email, required: false, autofocus: true, input_html: { autocomplete: "email" } 
            = f.input :password,   required: false,   input_html: { autocomplete: "current-password" } 
            = f.input :remember_me, as: :boolean if devise_mapping.rememberable?
          .form-actions
            = f.button :submit, "Log in", class: "btn btn-info"

        = render "devise/shared/links"

